// Primitivos (imutáveis) - string, number, boolean, undefined, null, (bigint, symbol) - Valores copiados

// Referemcia (mutável) - array, object, funciton - Passados por referência

// para copiar por referencia

let a = [1,2,3];
let = [...a];
let c = b;